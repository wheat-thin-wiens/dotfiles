[opener]
edit = [
  { run = 'nvim $@', block = true, desc = 'nvim', for = 'unix' }
]
sudoedit = [
  { run = 'nvim $@', block = true, desc = 'nvim', for = 'unix' }
]

# [plugin]
# prepend_previewers = [
#   { name = "*.*", exec = "bat" }
# ]
